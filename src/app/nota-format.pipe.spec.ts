import { NotaFormatPipe } from './nota-format.pipe';

describe('NotaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NotaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
